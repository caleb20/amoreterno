/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // Agrega aquí dominios externos si usas next/image con imágenes externas
  },
};

module.exports = nextConfig; 